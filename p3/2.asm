#中断子程序：
#比对输入设备和输出初值
lw $6,4($21)
lw $7,0($22)
beq $6,$7,label
#如果不一样，把输入给输出的初值
sw $7,4($21)
j ret
#如果一样，输出设备的当前值加1
label:
lw $5,0($21)
addiu $5,$5,1
sw $5,0($21)
ret:
ori $10,$0,0x000A   # 记多久的时间写多久？？
sw $10,4($20)
mtc0 $0,$13
eret